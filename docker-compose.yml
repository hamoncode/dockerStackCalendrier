services:
  db:
    image: mariadb:10.9
    restart: unless-stopped
    env_file: .env
    volumes:
      - db_data:/var/lib/mysql

  nextcloud:
    image: nextcloud:latest
    restart: unless-stopped
    env_file: .env
    depends_on:
      - db
    ports:
      - "8080:80"
    volumes:
      - nextcloud_data:/var/www/html
      - ./themes/my-school:/var/www/html/themes/my-school:ro
      - ./scripts/initialize.sh:/docker-entrypoint.d/initialize.sh:ro
      - calendar-data:/var/www/html/public_calendars:ro

  calendar-sync:
    build:
      context: ./sync
    restart: always
    # no env_file needed if your script just reads /data/calendar_feeds.env
    volumes:
      - calendar-data:/data
      - ./sync/calendar_feeds.env:/data/calendar_feeds.env:ro

volumes:
  db_data:
  nextcloud_data:
  calendar-data:

